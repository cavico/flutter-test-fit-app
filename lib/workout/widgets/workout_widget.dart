import 'package:flutter/material.dart';

class WorkoutWidget extends StatelessWidget {
  const WorkoutWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'WorkoutWidget is working',
      ),
    );
  }
}
